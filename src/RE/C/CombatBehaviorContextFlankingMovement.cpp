#include "RE/C/CombatBehaviorContextFlankingMovement.h"

namespace RE
{
	bool CombatBehaviorContextFlankingMovement::CheckShouldStalk()
	{
		using func_t = decltype(&CombatBehaviorContextFlankingMovement::CheckShouldStalk);
		REL::Relocation<func_t> func{ RELOCATION_ID(47118, 0) };  // I do not know for AE
		return func(this);
	}
}
