#include "RE/C/CombatBehaviorAcquireItem.h"

namespace RE
{
	void CombatBehaviorAcquireItem::Enter()
	{
		using func_t = decltype(&CombatBehaviorAcquireItem::Enter);
		REL::Relocation<func_t> func{ RELOCATION_ID(46349, 0) };  // I do not know for AE
		return func(this);
	}

	void CombatBehaviorAcquireItem::Update()
	{
		using func_t = decltype(&CombatBehaviorAcquireItem::Enter);
		REL::Relocation<func_t> func{ RELOCATION_ID(46350, 0) };  // I do not know for AE
		return func(this);
	}
}
