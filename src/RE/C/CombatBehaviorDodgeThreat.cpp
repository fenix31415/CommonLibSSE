#include "RE/C/CombatBehaviorDodgeThreat.h"

namespace RE
{
	void CombatBehaviorDodgeThreat::Enter()
	{
		using func_t = decltype(&CombatBehaviorDodgeThreat::Enter);
		REL::Relocation<func_t> func{ RELOCATION_ID(46590, 0) };  // I do not know for AE
		return func(this);
	}

	void CombatBehaviorDodgeThreat::Exit()
	{
		using func_t = decltype(&CombatBehaviorDodgeThreat::Exit);
		REL::Relocation<func_t> func{ RELOCATION_ID(46592, 0) };  // I do not know for AE
		return func(this);
	}

	void CombatBehaviorDodgeThreat::Update()
	{
		using func_t = decltype(&CombatBehaviorDodgeThreat::Update);
		REL::Relocation<func_t> func{ RELOCATION_ID(46591, 0) };  // I do not know for AE
		return func(this);
	}
}
