#include "RE/C/CombatBehaviorCircleDistant.h"

namespace RE
{
	void CombatBehaviorCircleDistant::Enter()
	{
		using func_t = decltype(&CombatBehaviorCircleDistant::Enter);
		REL::Relocation<func_t> func{ RELOCATION_ID(46728, 0) };  // I do not know for AE
		return func(this);
	}

	void CombatBehaviorCircleDistant::Update()
	{
		using func_t = decltype(&CombatBehaviorCircleDistant::Update);
		REL::Relocation<func_t> func{ RELOCATION_ID(46729, 0) };  // I do not know for AE
		return func(this);
	}
}
